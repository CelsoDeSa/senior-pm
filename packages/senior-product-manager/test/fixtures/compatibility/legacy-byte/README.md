# Synthetic legacy-byte fixture

This fixture is intentionally synthetic and minimal. It exercises frozen marker, schema-alias, wrapper, manifest, and migration-byte handling. It is not a product sample and must not be linked from public feature documentation.
