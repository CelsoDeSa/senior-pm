# Synthetic Senior PM sample

This directory contains an intentionally synthetic specification generated from a fictional local task. It contains no customer request, private repository evidence, operational roadmap, or production claim.

The generated artifact demonstrates immutable Senior PM output and the neutral `senior-pm.plugin` ownership marker. It is product documentation, not compatibility-fixture data.

The example intentionally stops at `Ready for design review`. It has no external design approval, is handoff ineligible, and must not be implemented or handed off.
